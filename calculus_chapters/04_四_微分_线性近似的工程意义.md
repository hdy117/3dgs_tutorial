# Ch04 - 微分：Euler 的“一切皆可线性化”哲学

> **本章目标**：理解为什么 PyTorch 的 `autograd` 不需要知道函数的全貌，只需要局部信息。
> **核心洞察**：**泰勒展开 (Taylor Expansion)** —— 无论函数多复杂，只要凑得够近，它看起来就像一条直线。这是所有数值优化的基石。

---

## 🔥 Part 0: Euler 的“显微镜”视角

### 为什么我们需要近似？

在 Ch03 中我们学了导数 $f'(x)$。但导数本身只告诉我们一个点的信息。
如果我们要做优化（比如找函数的最小值），或者要做计算机模拟，我们必须知道：**如果我移动了一小步 $\Delta x$，函数值会变成什么样？**

直接算很难：$f(x + \Delta x)$ 往往是一个极其复杂的表达式。

1748年，Leonhard Euler 提出了一个震撼世界的观点（《无穷分析引论》）：
> **"任何光滑的曲线，只要你凑得足够近看，它就是一条直线。"**

这就是**线性化 (Linearization)**。

---

## 🔥 Part 1: Taylor Expansion —— 函数的“翻译器”

### 核心公式：一阶泰勒展开

对于任意光滑函数 $f(x)$，当 $\Delta x$ 很小时：
$$f(x + \Delta x) \approx f(x) + f'(x) \cdot \Delta x$$

**Euler 的直觉翻译**：
*   $f(x)$：你现在的海拔高度。
*   $f'(x)$：你现在脚下的坡度（导数）。
*   $\Delta x$：你向前走的距离。
*   $f(x) + f'(x)\Delta x$：预测你走到那里的新高度。

**为什么有效？** 因为局部来看，曲线就是切线！

---

### 高阶项去哪了？（微分的诞生）

实际上，泰勒展开是一个无穷级数：
$$f(x + \Delta x) = f(x) + f'(x)\Delta x + \frac{1}{2}f''(x)(\Delta x)^2 + \dots$$

在 Euler 和 Leibniz 的直觉中，当 $\Delta x$（即 $dx$）是“无穷小”时：
*   $(dx)^2$ 比 $dx$ 还要小得多，可以忽略不计。
*   高阶项更是不值一提。

所以只保留前两项，就得到了**微分 (Differential)**：
$$df = f'(x) dx$$
或者写成预测式：
$$\Delta y \approx dy = f'(x)\Delta x$$

---

### ✅ 关键洞察：这是 PyTorch autograd 的底层逻辑！

你在 Ch03 看到的 $dy = f'(x)dx$，在 Euler 看来就是**线性近似**。
PyTorch 在做反向传播时，本质上是在做每一层函数的局部线性近似。它不需要知道全局形状，只需要知道“在这个点上，往哪走会最快”。

---

## 🔥 Part 2: 微分在 3DGS 中的工程意义

### 场景：参数更新的预测

在训练循环中，我们更新 Gaussian 参数 $\mu$：
$$\mu_{new} = \mu_{old} - \eta \cdot \frac{\partial L}{\partial \mu}$$

这行代码背后的微分逻辑是：
$$L(\mu_{new}) \approx L(\mu_{old}) + \frac{\partial L}{\partial \mu} (\mu_{new} - \mu_{old}) = L(\mu_{old}) - \eta \cdot \left|\frac{\partial L}{\partial \mu}\right|^2$$

**因为 $\eta$ 很小（比如 0.001），所以线性近似极其准确！**
这就是为什么梯度下降有效 —— **我们总是假设自己在局部“下坡”**。

---

### 局限性：当“局部”不再小

如果学习率 $\eta$ 太大，或者函数本身极度弯曲（非凸），一阶线性近似就会失效：
$$f(x + \Delta x) \neq f(x) + f'(x)\Delta x$$
此时我们需要二阶导数（Hessian Matrix）来修正曲率 —— 这就是 Ch05 进阶题提到的 Hessian。

---

## 🔥 Part 3: 常见微分速查表（工程版）

| 函数 $f(x)$ | 微分 $df$ (变化量预测) | 3DGS 中的应用场景 |
|-----------|---------------------|-----------------|
| $x^2$ | $2x dx$ | MSE Loss 中梯度随预测值的变化 |
| $e^x$ | $e^x dx$ | Alpha blending 中的透明度累积 |
| $\ln x$ | $\frac{1}{x} dx$ | 正则化项的梯度更新 |
| $f(g(x))$ | $df = f'(g)dg$ | **链式法则的核心操作**（微分形式） |

---

## 🔥 Part 4: 严格推导 — 为什么高阶项可以忽略？

原文只说了"$(dx)^2$ 很小所以忽略"，但这不够严谨。我们用 **Big-O 记号** 来量化它。

### 证明目标
设 $f(x)$ 连续可导（$C^1$）。我们要证明当 $\Delta x \to 0$ 时：
$$f(x + \Delta x) = f(x) + f'(x)\Delta x + o(\Delta x)$$
即：**余项的量级严格小于线性项**。

### 证明过程

**Step 1 — 引入误差函数（定义）**
令 $R(\Delta x)$ 为泰勒展开的余项：
$$R(\Delta x) = f(x + \Delta x) - [f(x) + f'(x)\Delta x]$$

**Step 2 — 验证量级关系**
根据导数的定义，误差项满足：
$$\lim_{\Delta x \to 0} \frac{R(\Delta x)}{\Delta x} = 0$$
这意味着 $R(\Delta x)$ 是 $\Delta x$ 的**高阶无穷小**（记为 $o(\Delta x)$）。

### ✅ 具体数值验证（以 $f(x) = x^2$ 为例）

假设当前在 $x=3$，步长 $\eta = \Delta x = 0.001$。

- **线性项预测的变化**：
$$\text{Linear} = f'(3) \cdot \Delta x = 6 \times 0.001 = 0.006$$
- **实际变化量**：
$$\Delta y = (3.001)^2 - 3^2 = 9.006001 - 9 = 0.006001$$
- **余项（误差）**：
$$R(0.001) = \text{实际} - \text{线性预测} = 0.006001 - 0.006 = 0.000001$$

**关键洞察**：
- 线性项的量级是 $O(\Delta x) = O(10^{-3})$。
- 余项的量级是 **$O((\Delta x)^2) = O(10^{-6})$**。

### 🛠️ 对 PyTorch autograd 的工程意义

在梯度下降中：$\theta_{new} = \theta - \eta \nabla f$。
当 $\eta$（学习率）取 $10^{-3}$ 时，线性项主导了参数的移动方向，而误差项只有线性项的百万分之一量级。这就是为什么**只要学习率足够小，一阶近似就极其可靠**。∎

---

## 📚 习题

### ✅ 基础题（必做）

**4.1 Euler 线性化实战 —— 估算变化量**

设 $f(x) = x^2$，当前在 $x=3$ 处。
(a) 计算导数 $f'(3)$。
(b) 如果 $\Delta x = 0.1$（向正方向走一小步），用微分公式 $\Delta y \approx f'(x)\Delta x$ 估算新的函数值。
(c) 实际值是多少？误差有多大？

(a) $f'(x) = 2x \implies f'(3) = 6$.  
(b) $\Delta y \approx 6 \times 0.1 = 0.6$。预测新值 $9 + 0.6 = 9.6$。  
(c) 实际值 $(3.1)^2 = 9.61$。误差仅为 $0.01$（这就是 $(dx)^2/2$ 的贡献）。

---

**4.2 为什么忽略高阶项是合理的？** (First Principles)

(a) 当 $dx \to 0$ 时，证明 $\frac{(dx)^2}{dx} \to 0$。
(b) 这说明 $(dx)^2$ 比 $dx$ “小多少倍”？

(a) 极限为 0。  
(b) 当变量趋近于 0 时，平方项消失得更快（高阶无穷小）。所以在极小的局部范围内，线性项 $dx$ 占绝对主导。

---

### 🔥 进阶题（选做）

**4.3 3DGS 中的“非线性”陷阱**

在 alpha blending 中，最终颜色是透明度的复杂函数：$C = c_1\alpha_1 + c_2(1-\alpha_1)\alpha_2 \dots$。
如果在某一步更新时 $\Delta \alpha$ 很大（比如初始阶段），为什么线性近似可能失效？

因为 Alpha blending 包含乘积项，函数本身是非线性的。当步长很大时，$(d\alpha)^2, (d\alpha)^3$ 等项不可忽略，导致预测的 loss 变化与实际不符。这就是为什么训练初期需要较小的学习率或自适应优化器（如 Adam）。

---

## 🔗 下一章

[→ Ch05: 偏导数与梯度](./05_五_偏导数与梯度_.md) （从一维线性近似到多维导航仪）

<div align="center">
**🔥 Ember's Note**: 微分 = "局部直线化"。  
→ **建议**：理解为什么 PyTorch 可以忽略高阶项，这是理解整个深度学习优化的前提。
</div>

---

## 🧠 深度练习 (Deep Practice)

### 1. Linearization Logic — 局部直线化
**问题**: Euler 的“一切皆可直线化”哲学在优化中意味着什么？

💡 **Hint**: 只要步子迈得够小，曲线就是切线。

✅ **Answer**: 
1. **核心逻辑**: 无论函数多复杂（如非凸 Loss），在当前点附近取 $\Delta x$ 极小时，它可以用一阶泰勒展开完美代替：$f(x) \approx f(x_0) + f'(x_0)(x-x_0)$。
2. **直觉解释**: 这意味着我们不需要知道全局形状，只需要在脚下踩的那个小点建立局部坐标系，就能通过线性导航找到方向。

---

### 2. Taylor Formula — 预测公式
**问题**: 写出 $f(x)$ 在 $x_0$ 处的一阶泰勒展开公式。各项分别代表什么？

💡 **Hint**: $L(x) = f(x_0) + f'(x_0)(x-x_0)$。

✅ **Answer**: 
1. **核心公式**: 第一项 $f(x_0)$ 是**起点高度**；第二项 $f'(x_0)\Delta x$ 是基于**当前坡度（导数）**的预测增量。
2. **直觉解释**: 就像看天气预报：知道现在的气温，加上“每小时升温几度”乘以“预计经过的时间”，就能预测未来的气温。

---

### 3. Higher Order Terms — 曲率修正
**问题**: 二阶泰勒展开加上了哪一项？这一项在物理上对应什么？

💡 **Hint**: $\frac{1}{2}f''(x_0)(x-x_0)^2$。对应加速度/曲率。

✅ **Answer**: 
1. **核心公式**: 二次项 $rac{1}{2} f'' \Delta x^2$。
2. **直觉解释**: 一阶假设我们匀速直线运动；二阶考虑到了**“弯曲”**（即抛物线轨迹）。在优化中，这对应 Hessian 矩阵的信息，帮助我们在陡峭处减速、平缓处加速。

---

### 4. Differential Definition — dy vs $\Delta$y
**问题**: 微分 $dy$ 和函数的变化量 $\Delta y$ 有什么区别？

💡 **Hint**: $dy = f'(x)dx$ 是线性部分（切线上）；$\Delta y$ 是非线性部分（曲线上）。

✅ **Answer**: 
1. **核心定义**: $dy$ 是函数在切线上的增量（预测值），而 $\Delta y$ 是实际曲线上的增量。
2. **直觉解释**: 它们之间的误差由高阶项 $(dx)^2$ 主导。当 $dx 	o 0$ 时，$\Delta y \approx dy$。**这是微积分工程化的核心：用线性代替非线性**。

---

### 5. Error Order Analysis — Big-O
**问题**: 证明当 $h 	o 0$ 时，$(x+h)^2 - x^2 = 2xh + h^2$. 说明为什么一阶近似在极小步长下极其准确？

💡 **Hint**: $h^2$ 远小于 $h$。

✅ **Answer**: 
1. **核心推导**: $\Delta y = 2xh + h^2$；线性预测 $dy = 2xh$。误差 $R(h) = h^2$。
2. **直觉解释**: 如果步长缩小 10 倍，线性项也缩小 10 倍，但**误差项会缩小 100 倍**！这就是为什么梯度下降在小学习率下非常精确的原因。

---

### 6. Alpha Blending Non-Linearity — 乘积效应
**问题**: 在 Alpha blending 中，如果 $\Delta lpha$ 很大，一阶近似会失效？

💡 **Hint**: 乘积项导致二阶、三阶项显著。

✅ **Answer**: 
1. **核心逻辑**: Blending 公式包含 $lpha_i \cdot lpha_j$ 等乘法项（非线性）。
2. **直觉解释**: 当步子迈大时，这些高阶乘积的贡献不可忽略。预测的 Loss 变化与实际严重不符。**这解释了为什么优化初期必须使用较小的学习率或自适应算法**。

---

### 7. Predicting New Value — $x^2$ 估算
**问题**: 已知 $f(x)=x^2$, 在 $x=3$ 处。用微分公式估算 $f(3.01)$ 的值。实际值是多少？

💡 **Hint**: $\Delta y \approx f'(3) \cdot 0.01 = 6 \cdot 0.01 = 0.06$. 

✅ **Answer**: 
1. **预测结果**: $9 + 0.06 = 9.06$。
2. **实际值**: $(3.01)^2 = 9.0601$。
3. **结论**: 误差仅为 $0.0001$！这展示了微分在工程估算中的强大威力（百万分之一精度）。

---

### 8. Taylor in Optimization — 牛顿法
**问题**: 在牛顿法 (Newton's Method) 中，为什么要用到二阶泰勒展开？

💡 **Hint**: 利用曲率信息加速收敛。

✅ **Answer**: 
1. **核心逻辑**: 通过二阶项（Hessian），我们可以拟合出一个抛物线 $f(x) pprox ax^2+bx+c$，并直接求出其谷底解析解：$\Delta x = -b/2a$。
2. **直觉解释**: 这叫**二次收敛**。它不像梯度下降那样“盲走”，而是利用曲率信息“一步到位”跳到最优点附近。

---

### 9. Lagrange Remainder — 误差上界
**问题**: 泰勒展开的拉格朗日余项 $R_n(x)$ 表达了什么？

💡 **Hint**: $rac{f^{(n+1)}(c)}{(n+1)!}(x-x_0)^{n+1}$。截断误差的上界。

✅ **Answer**: 
1. **核心意义**: 它给出了**近似有多准**的数学保证。
2. **直觉解释**: 只要我们知道函数的高阶导数有界，并且步长 $(x-x_0)$ 足够小，我们就可以确信这个近似的误差不超过某个范围。

---

### 10. Exponential Function DNA — $e^x$
**问题**: 写出 $e^x$ 在 $x=0$ 处的泰勒展开式（麦克劳林级数）。

💡 **Hint**: $(e^x)^{(n)} = e^x$, 且在 0 处值为 1. 

✅ **Answer**: 
1. **核心公式**: $1 + x + rac{x^2}{2!} + rac{x^3}{3!} + ...$
2. **直觉解释**: 它是指数增长的唯一模型。在计算机中，CPU/GPU 算 $e^{0.5}$ 时内部其实就是在做这些多项式的累加运算。

---

### 11. Sine Function DNA — $\sin x$
**问题**: 写出 $\sin(x)$ 在 $x=0$ 处的泰勒展开式（前 4 项）。

💡 **Hint**: 奇函数，只有奇次幂。

✅ **Answer**: 
1. **核心公式**: $x - rac{x^3}{6} + rac{x^5}{120} - ...$
2. **直觉解释**: 当 $x$ 很小时（如弧度制下的微小角度），$\sin x \approx x$。这就是微积分里“小角近似”的数学根源。

---

### 12. Cosine Function DNA — $\cos x$
**问题**: 写出 $\cos(x)$ 在 $x=0$ 处的泰勒展开式（前 4 项）。

💡 **Hint**: 偶函数，只有偶次幂。

✅ **Answer**: 
1. **核心公式**: $1 - rac{x^2}{2} + rac{x^4}{24} - ...$
2. **直觉解释**: 在 $x=0$ 附近（波峰），余弦函数的变化率很小，且呈现向下弯曲（二阶导数为负）。

---

### 13. Local vs Global — 逼近失效
**问题**: 为什么泰勒展开被称为“局部”近似？当 $x$ 远离 $x_0$ 时会发生什么？

💡 **Hint**: 高阶项权重随距离增加而急剧增大。

✅ **Answer**: 
1. **核心逻辑**: 多项式 $(x-x_0)^n$ 在 $x 	o x_0$ 时收敛，但在远离时发散。
2. **直觉解释**: 就像你用放大镜看地球表面，它是平的（局部近似）；但如果你飞离地球很远，它就变圆了（全局非线性）。

---

### 14. Numerical Differentiation — 中心差分
**问题**: 如何仅使用泰勒展开推导中心差分公式 $rac{f(x+h)-f(x-h)}{2h} = f'(x) + O(h^2)$？

💡 **Hint**: $f(x+h)$ 和 $f(x-h)$ 的级数相减。

✅ **Answer**: 
1. **核心推导**: $(f+hf'+h^2/2...) - (f-hf'+h^2/2...) = 2hf' + O(h^3)$。
2. **直觉解释**: 两个泰勒展开相加会抵消奇次项（保留误差），相减则会抵消偶次项。**中心差分比单侧差分更精确**（因为二阶误差被抵消了）。

---

### 15. Hessian as Curvature — 矩阵形式
**问题**: 在多变量情况下，二次泰勒展开的矩阵形式是什么？

💡 **Hint**: $\Delta \theta^T H \Delta \theta$. 

✅ **Answer**: 
1. **核心公式**: $L(\theta) \approx L_0 + g^T\Delta	heta + rac{1}{2}\Delta	heta^T H \Delta	heta$。
2. **直觉解释**: 其中 $H$ 是 Hessian 矩阵（二阶偏导数），描述了多维空间中 Loss 曲面的弯曲程度。**它是优化器调整步长的“地形雷达”**。

---

### 16. Linear Approximation Error — 直线函数
**问题**: 如果函数本身是一条直线 $f(x) = ax+b$, 泰勒展开的一阶近似误差是多少？

💡 **Hint**: 零 (0)。

✅ **Answer**: 
1. **核心逻辑**: 因为高阶导数均为 0（直线的斜率不变）。
2. **直觉解释**: 对于线性函数，一阶泰勒展开就是**精确解**。不需要任何修正项。

---

### 17. Sigmoid Linearization — 零点附近
**问题**: 在 Sigmoid 的零点 ($x=0$), 它的切线斜率（导数）是多少？

💡 **Hint**: $\sigma'(0) = \sigma(0)(1-\sigma(0))$. 

✅ **Answer**: 
1. **核心结果**: $0.5 * (1-0.5) = 0.25$。
2. **直觉解释**: 所以在 0 点附近，Sigmoid 可以近似看作线性函数 $y pprox 0.5 + 0.25x$。**这是神经网络初始化时避免梯度消失的关键原因之一**（让激活值靠近零点）。

---

### 18. Predictive Control — MPC
**问题**: 在模型预测控制 (MPC) 中，为什么需要对系统进行泰勒展开？

💡 **Hint**: 建立局部线性动态模型。

✅ **Answer**: 
1. **核心逻辑**: 非线性系统（如汽车动力学）很难直接优化。
2. **直觉解释**: 通过在当前工作点附近做一阶或二阶展开，我们在短期预测窗口内将其“视为”线性系统，从而可以用二次规划 (QP) 求解出最优动作序列。

---

### 19. Function Approximation Theory — 魏尔斯特拉斯
**问题**: 魏尔斯特拉斯逼近定理告诉我们什么关于泰勒级数的真理？

💡 **Hint**: 任何连续函数都可以用多项式无限逼近。

✅ **Answer**: 
1. **核心意义**: 这从理论上保证了泰勒展开（多项式）作为计算工具的强大能力——只要项数够多，它就能“翻译”任意复杂的波形，哪怕是方波或锯齿波。

---

### 20. Exponential Taylor — 极限证明
**问题**: 利用泰勒展开推导 $\lim_{h	o 0} rac{e^h - 1}{h} = 1$. 

💡 **Hint**: $e^h \approx 1 + h + h^2/2...$。

✅ **Answer**: 
1. **核心推导**: $( (1+h+...) - 1 ) / h = 1 + h + ... 	o 1$。
2. **直觉解释**: 这证明了 $e^x$ 在 $x=0$ 处的切线斜率（导数）是 1，完美符合“增长速率等于自身”的定义。
