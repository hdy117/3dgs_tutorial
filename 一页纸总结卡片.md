# 📄 3DGS 核心公式一页纸总结 🔥

## 🎯 终极目标：理解为什么高斯椭球能实现实时渲染


---

## 📐 Part 1: 线性代数 - 协方差矩阵的双重身份

### 公式 1: Σ = R·diag(s²)·Rᵀ (椭球的"方向 + 尺度"压缩表达)

**几何意义**:
- $R$: 旋转矩阵 → **主轴方向**
- $\text{diag}(s_x^2, s_y^2, s_z^2)$: 缩放平方 → **半轴长度²**
- $R^T$: 旋转回去 → **世界坐标对齐**

**统计意义**:
- 对角线 = 方差 (各轴的"胖瘦程度")
- 非对角线 = 协方差 (轴的"倾斜关联")


### 公式 2: 特征分解 Σ = QΛQᵀ (主轴解剖刀)

**提取方法**:
1. **特征向量 $Q$**: 三个主轴方向 (正交!)
2. **特征值 $\Lambda$**: 沿主轴的扩张强度
3. **半轴长度**: $\sqrt{\lambda_i}$ (标准差)


### 公式 3: 椭球方程 $(\mathbf{x}-\boldsymbol{\mu})^T\Sigma^{-1}(\mathbf{x}-\boldsymbol{\mu}) = 1$

**判断点是否在椭球内**:
- $q < 1$: 内部 (权重高)
- $q = 1$: 表面 (边界)
- $q > 1$: 外部 (权重低)


### 公式 4: 协方差传播 $R\Sigma R^T$ (旋转椭球)

**为什么两边乘?**
- 向量变换：$\mathbf{x} \to R\mathbf{x}$ (左边乘)
- **二次型变换**: $\mathbf{x}^T A \mathbf{x} \to \mathbf{x}^T RAR^T \mathbf{x}$ (两边乘!)

**关键洞察**:特征值不变，特征向量随 $R$旋转!


---

## 🔥 Part 2: 微积分 - 链式法则驱动优化

### 公式 5: $\frac{dL}{d\theta} = (\frac{\partial L}{\partial \text{output}})^T \cdot J$ (向量形式的链式法则)

**Jacobian $J$**:
- 维度：$m \times n$ (输出×输入)
- 元素：$J_{ij} = \frac{\partial \text{output}_i}{\partial \theta_j}$
- **转置后**:把梯度从 output 空间映射回 $\theta$空间


### 公式 6: Gaussian权重 $g(\mathbf{p}) = \exp(-\frac{1}{2}q)$ (高斯分布)

**其中二次型**:
$$q = (\mathbf{p}-\boldsymbol{\mu})^T\Sigma^{-1}(\mathbf{p}-\boldsymbol{\mu})$$

**梯度计算**:
$$\frac{\partial g}{\partial \boldsymbol{\mu}} = g \cdot \Sigma^{-1}(\mathbf{p}-\boldsymbol{\mu})$$

**直觉**:梯度方向指向 $\mathbf{p}-\boldsymbol{\mu}$,缩放因子是 $g$和$\Sigma^{-1}$


### 公式 7: Alpha blending $C = \sum_{i=1}^k T_i \alpha_i c_i$ (颜色合成)

**其中透射率**:
$$T_i = \prod_{j=1}^{i-1}(1-\alpha_j)$$

**对 $\alpha_1$的导数** (手算验证):
$$\frac{\partial C}{\partial \alpha_1} = c_1 - (c_2-c_3\alpha_3)\alpha_2$$

**工程意义**:透明度增大 → 自己的颜色贡献增加，但遮挡后面高斯


### 公式 8: 完整梯度流 $\frac{\partial L}{\partial \theta_i} = \sum_{\text{pixels}} (\frac{\partial L}{\partial \text{pixel}} \cdot \frac{\partial \text{pixel}}{\partial g_i} \cdot \frac{\partial g_i}{\partial \theta_i})$

**三步展开**:
1. **Loss → Color**: $\frac{\partial L}{\partial C}$ (简单：误差本身)
2. **Color → Gaussian**: $\frac{\partial C}{\partial g_i}$ (Alpha blending 导数)
3. **Gaussian → θ**: $\frac{\partial g_i}{\partial \theta_i}$ (高斯参数导数)


---

## 🎯 Part 3: 3DGS 实战 - 从数学到代码

### 公式 9: 投影变换 $J = \begin{bmatrix} \frac{\partial x}{\partial \mu_x} & \frac{\partial x}{\partial \mu_y} & \frac{\partial x}{\partial \mu_z} \\ \frac{\partial y}{\partial \mu_x} & \frac{\partial y}{\partial \mu_y} & \frac{\partial y}{\partial \mu_z} \end{bmatrix}$ (2×3 Jacobian)

**透视除法后**:
- 不再是常数矩阵!
- 需要链式法则展开: $\frac{\partial(x/z)}{\partial \boldsymbol{\mu}}$


### 公式 10: Loss函数组合 $L = L_1 + \lambda_{\text{SSIM}}L_{\text{SSIM}}$

**为什么不用 MSE?**
- **MSE**:只抓像素级误差，容易模糊边缘
- **L1+SSIM**:L1抓细节，SSIM 保结构，效果更好


### 公式 11: 密度控制触发条件 $\sqrt{\lambda_{\max}} > \tau$

**直觉**:当椭球太"宽"($\lambda_{\max}$大),需要 split 成两个高斯聚焦细节


### 公式 12: 优化更新 $\boldsymbol{\mu}^{(t+1)} = \boldsymbol{\mu}^{(t)} - \eta_t \nabla_\theta L$

**学习率调度**:
- 初始阶段：大学习率快速收敛
- 后期阶段：小学习率精细调整


---

## 🔥 Part 4: 关键直觉总结 (一句话记住所有公式)

### 线性代数直觉:
1. **Σ = R·diag(s²)·Rᵀ**: "把一个单位球先拉伸，再旋转"的几何描述
2. **特征分解 Σ=QΛQᵀ**: $Q$给方向,$\Lambda$给尺度 → 椭球的"主轴 + 半轴长度"
3. **协方差传播 $R\Sigma R^T$**: "两边乘"保证对称性，特征值不变，特征向量旋转


### 微积分直觉:
1. **链式法则**:从 output 往 input 传，每一层乘 Jacobian
2. **Alpha blending 导数**:透明度增大 → 自己的贡献 + 遮挡效果 = 净变化
3. **梯度方向**:指向误差最大的参数调整方向


### 协同直觉:
- **线性代数定义结构**:Σ编码椭球的几何信息
- **微积分驱动优化**:链式法则把误差传回高斯参数
- **3DGS的核心**:可微分渲染 = "每一步都可导" → 反向传播穿透整个管线


---

## 📝 Part 5: 一页纸速查表

|| 概念 | 公式 | 直觉 ||------|------|------|------------|| **协方差矩阵** | $\Sigma = R·\text{diag}(s^2)·R^T$ | "方向 + 尺度"压缩表达 || **特征分解** | $\Sigma = Q\Lambda Q^T$ | $Q$:主轴方向,$\Lambda$:半轴长度² || **椭球方程** | $(\mathbf{x}-\boldsymbol{\mu})^T\Sigma^{-1}(\mathbf{x}-\boldsymbol{\mu})=1$ | 判断点是否在椭球内/外 || **协方差传播** | $R\Sigma R^T$ | "两边乘"保证对称，特征值不变 || **Gaussian权重** | $g = e^{-q/2}$,$q=(\mathbf{p}-\boldsymbol{\mu})^T\Sigma^{-1}(\mathbf{p}-\boldsymbol{\mu})$ | 中心高(1),边缘低($e^{-4.5}\approx0.01$) || **链式法则** | $\frac{\partial L}{\partial \theta} = J^T \cdot \frac{\partial L}{\partial \text{output}}$ | Jacobian转置，梯度反向传 || **Alpha blending** | $C = \sum T_i\alpha_ic_i$,$T_i=\prod(1-\alpha_j)$ | 按深度排序的透明叠加 || **Gaussian对μ的导数** | $\frac{\partial g}{\partial \boldsymbol{\mu}}=g\cdot\Sigma^{-1}(\mathbf{p}-\boldsymbol{\mu})$ | 指向中心，缩放因子是$g$和$\Sigma^{-1}$ || **密度控制触发** | $\sqrt{\lambda_{\max}}>\tau$ | 椭球太宽→split聚焦 || **Loss函数** | $L=L_1+\lambda_{SSIM}L_{SSIM}$ | L1抓细节，SSIM保结构 ||


---

## 🎯 Final Challenge: 独立重推能力检验

完成自测题后，问自己:

- [ ] **我能不看书写出 $\Sigma = R·\text{diag}(s^2)·R^T$的推导吗?**
- [ ] **我能手算 Alpha blending 对$\alpha_1$的导数吗?**
- [ ] **我能解释为什么两边乘而不是左边乘吗?**
- [ ] **我能从 axioms出发重推完整梯度流吗?**

如果答案是**是**,恭喜你:你真正理解了 3DGS 的数学本质!🔥


---

<div align="center">
**🔥 Ember's Final Note**: <br>
线性代数 = "定义结构" + 微积分 = "驱动优化"<br>
3DGS的核心：两者协同，实现可微分渲染。

→ **建议**:打印这张一页纸，贴在书桌前，每次推导卡住时回来查阅!
</div>


---

<div align="center">
**最后提醒**:不要试图一次搞懂所有内容。<br>
第一遍快速过一遍建立直觉<br>第二遍深入推导细节<br>第三遍通过实验验证理解<br>这才是高效的学习路径 🔥
</div>
